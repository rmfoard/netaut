count=1
while [ $count -le $2 ]; do
    ./explorer \
    --rule `./randrule --randseed $RANDOM` \
    --no-write-end-state \
    --nodes $1 --iterations $1 --cycle-check-depth $1 $* >> $3.json
    echo "" >> $3.json
    ((count++))
done
